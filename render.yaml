# Render Blueprint — https://render.com/docs/blueprint-spec
# Ensures the API is built from /backend (installs the right package.json).
services:
  - type: web
    name: attendance-api
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
