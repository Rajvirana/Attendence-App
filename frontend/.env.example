# Production (Vercel): your Render backend URL, no trailing slash
# Example: https://your-api.onrender.com
# Leave empty in local dev — Vite proxies /api and /socket.io to localhost:5000
VITE_API_BASE_URL=
