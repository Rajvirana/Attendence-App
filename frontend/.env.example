# Local dev: leave empty — Vite proxies /api and /socket.io to localhost:5000
#
# Vercel production: set to your Render API (no trailing slash), e.g.:
# VITE_API_BASE_URL=https://attendence-app-jlgs.onrender.com
VITE_API_BASE_URL=
